<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vitoroliveira.barber">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".funcionario.FuncionarioActivity" />
        <activity android:name=".cliente.CadastroClienteActivity" />
        <activity android:name=".servico.ServicoActivity" />
        <activity android:name=".cliente.ListClienteActivity" />
        <activity android:name=".servico.ListServicoActivity" />
        <activity android:name=".administrador.CadastroAdministradorActivity" />
        <activity android:name=".administrador.ListAdministradorActivity"></activity>
    </application>

</manifest>